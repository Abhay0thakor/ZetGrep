# Generic CSV Configuration
# Use this for data dumps where each row is an entity
format: csv
id: "0" # If has_header: true, use column name. If false, use index string.
targets:
  - "1"
  - "2"
csv_config:
  separator: ","
  has_header: false
  id_index: 0
  target_indices: [1, 2]
